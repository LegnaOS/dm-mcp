-- DM3 游戏主配置文件
-- 编码：GBK

local 配置 = {
    -- 基础设置
    游戏标题 = "{{GAME_TITLE}}",
    屏幕宽度 = {{SCREEN_WIDTH}},
    屏幕高度 = {{SCREEN_HEIGHT}},
    
    -- 资源配置
    资源表 = {
        -- 在这里注册游戏资源
        -- 示例：
        -- ["主角图片"] = "资源/图片/hero.png",
        -- ["背景音乐"] = "资源/音乐/bgm.mp3",
    },
    
    -- 默认字体
    默认字体 = {
        字体 = "宋体12",
        间距 = 0
    },
    
    -- 扩展组件
    扩展组件 = {
        -- 在这里注册自定义组件
    },
    
    -- 系统事件
    系统事件 = {
        -- 游戏开始事件
        游戏开始 = function()
            调试输出("游戏开始")
            
            -- 在这里初始化游戏
            -- 示例：
            -- 引擎.创建地图("主城")
            -- 引擎.显示窗口("主界面")
        end,
        
        -- 游戏结束事件
        游戏结束 = function()
            调试输出("游戏结束")
            
            -- 在这里清理资源
        end,
        
        -- 游戏更新事件（每帧调用）
        游戏更新 = function(间隔时间)
            -- 在这里处理游戏逻辑
        end,
        
        -- 按键按下事件
        按键按下 = function(按键码)
            调试输出("按键按下: " .. 按键码)
            
            -- 在这里处理按键输入
            return false
        end,
        
        -- 按键释放事件
        按键释放 = function(按键码)
            -- 在这里处理按键释放
            return false
        end,
        
        -- 鼠标按下事件
        鼠标按下 = function(按钮, x, y)
            -- 在这里处理鼠标点击
            return false
        end,
        
        -- 鼠标移动事件
        鼠标移动 = function(x, y)
            -- 在这里处理鼠标移动
            return false
        end
    }
}

return 配置
