-- DM3 服务器配置文件（可选）
-- 编码：GBK

local 配置 = {
    -- 服务器设置
    端口 = 8888,
    最大连接数 = 100,
    
    -- 事件处理
    事件 = {
        -- 服务器启动
        启动时 = function()
            调试输出("服务器启动")
        end,
        
        -- 客户端连接
        客户端连接 = function(客户端)
            调试输出("客户端连接: " .. 客户端.地址)
        end,
        
        -- 客户端断开
        客户端断开 = function(客户端)
            调试输出("客户端断开: " .. 客户端.地址)
        end,
        
        -- 接收消息
        接收消息 = function(客户端, 消息)
            调试输出("收到消息: " .. 消息)
            
            -- 在这里处理客户端消息
        end
    }
}

return 配置
